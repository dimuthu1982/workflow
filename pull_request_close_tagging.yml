name: PR Merge Demo
run-name: PR MErge GitHub Actions 🚀
on: pull_request
  types: 
    - closed
  branches:
    'master'
jobs:
  run_if:
    if:  startsWith(github.head_ref, 'releases/')
    runs-on: ubuntu-latest
    steps:
      - run: echo "The head of this PR starts with 'releases/'"
      - run: echo "Event Name (${{ github.event_name }}), HeadRef (${{ github.event_name }})"